<Page
    x:Class="PIC_Simulator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PIC_Simulator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListView ItemsSource="{x:Bind Sourcecode}" Grid.Column="1" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ProcessorInstruction">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Mnemonic}"/>
                        <TextBlock Text="{x:Bind Operands[0]}"/>
                        <TextBlock Text="{x:Bind Operands[1]}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.Content>
                <TextBlock Text="Statusbar" Margin="12,14" />
            </CommandBar.Content>
            <AppBarButton Icon="Play" Label="Start" />
            <AppBarButton Icon="Stop" Label="Halt" />
            <AppBarButton Icon="Forward" Label="Schritt" />
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Help" Label="Hilfe" Click="HelpButton_Click" />
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
